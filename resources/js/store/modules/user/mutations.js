export default {
    SET_USER (store, user) {
        store.user = user;
    },
    SET_ATTlOG (store, attLog) {
        store.attLog = attLog;
    },
    SET_PUNCH (store, punchLog) {
        store.punchLog = punchLog;
    }
    
}